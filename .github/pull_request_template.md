## Summary

<!-- What does this PR change and why? -->

## Changes

<!-- Bullet list of user-visible or developer-visible changes. -->

## Test plan

<!-- How did you verify the change? `cargo test --workspace`? Manual GUI smoke test? -->

## Checklist

- [ ] `cargo fmt --all -- --check` is clean
- [ ] `cargo clippy --workspace --all-targets -- -D warnings` is clean
- [ ] `cargo test --workspace` passes locally
- [ ] If a new translation key was added, every `crates/sourcevault-gui/i18n/*.json` was updated
- [ ] CHANGELOG.md updated under `## [Unreleased]`
